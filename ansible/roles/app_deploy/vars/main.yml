# 固定パスではなく、hostsで定義した deploy_root を使う
release_dir: "{{ deploy_root }}/releases/{{ version }}"
current_link: "{{ deploy_root }}/current"
# バックアップパスも各ホストのディレクトリ内に
backup_path: "{{ deploy_root }}/previous_release_path"
